# vi:syntax=bash

# OPAM configuration from $ opam init (should it be after or before the exports ?)
. /Users/hugobenichi/.opam/opam-init/init.sh > /dev/null 2> /dev/null || true

# OPAM env variables config from $ opam init
eval `opam config env`

# wrap ocaml with rlwrap for line editing and history
alias ocaml="rlwrap /usr/local/bin/ocaml"
